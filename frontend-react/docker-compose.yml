services:
  react-app:
    image: node:24-slim
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules 
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"